// use opencv::{self as cv, prelude::*};

fn read_image_cv() {
    // let img = cv::imgcodecs::imread(String::from("im0.png")).unwrap_or_else(|err| {
    //     eprintln!("Problem reading image: {err}");
    //     process::exit(1);
    // });
}
